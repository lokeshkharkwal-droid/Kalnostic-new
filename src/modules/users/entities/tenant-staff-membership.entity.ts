import { TenantStaffMembership } from '@prisma/client';

/** Domain/response shape for a tenant staff membership (Prisma is the source of truth). */
export type TenantStaffMembershipEntity = TenantStaffMembership;
