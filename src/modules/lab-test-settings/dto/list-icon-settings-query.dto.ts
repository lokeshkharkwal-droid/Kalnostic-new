import { PaginationQueryDto } from '../../../common/dto/pagination-query.dto';

/** Query params for listing icon settings (offset pagination only, for now). */
export class ListIconSettingsQueryDto extends PaginationQueryDto {}
