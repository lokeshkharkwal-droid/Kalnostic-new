import { PaginationQueryDto } from '../../../common/dto/pagination-query.dto';

/** Query params for listing image settings (offset pagination only, for now). */
export class ListImageSettingsQueryDto extends PaginationQueryDto {}
