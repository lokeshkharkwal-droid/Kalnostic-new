import { PaginationQueryDto } from '../../../common/dto/pagination-query.dto';

/** Query params for listing PDF settings (offset pagination only, for now). */
export class ListPdfSettingsQueryDto extends PaginationQueryDto {}
