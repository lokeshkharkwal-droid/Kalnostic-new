import { OverallResultTemplate } from '@prisma/client';

/**
 * Domain/response shape for an Overall Result template. The Prisma model is
 * the DB source of truth.
 */
export type OverallResultTemplateEntity = OverallResultTemplate;
