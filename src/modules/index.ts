// All feature modules live in this folder, one sub-folder each, following the
// scaffold in SKILL.md §1 (module / controller / service / dto / entities).
//
// As features are added, export their modules here and register them in
// AppModule, e.g.:
//   export * from './auth/auth.module';
//   export * from './user/user.module';
//
// Placeholder so the folder is tracked; remove once the first module lands.
export {};
