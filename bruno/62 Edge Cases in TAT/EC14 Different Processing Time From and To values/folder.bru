meta {
  name: EC14 Different Processing Time From and To values
  seq: 15
}

script:pre-request {
  if (!bru.getVar("baseUrl")) bru.setVar("baseUrl", "http://localhost:3000/api/v1");
}

docs {
  Scenario: different windows per test.
Expected: each test starts/pauses at its own From/To.
Status: VERIFIED by unit test. MUTATES config.

  Run 00 Base Setup first (same session) so the shared vars are available.
}
