meta {
  name: Verify TAT
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/lab-reports/{{labReportId}}/tat
  body: none
  auth: bearer
}

auth:bearer {
  token: {{operatorToken}}
}

assert {
  res.status: eq 200
}

docs {
  No TAT clock was started (tat_start_at null).
}
