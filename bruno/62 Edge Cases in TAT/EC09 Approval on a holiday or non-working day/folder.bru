meta {
  name: EC09 Approval on a holiday or non-working day
  seq: 10
}

script:pre-request {
  if (!bru.getVar("baseUrl")) bru.setVar("baseUrl", "http://localhost:3000/api/v1");
}

docs {
  Scenario: approve on a non-working day.
Expected: same as after-hours: final net = accrued total; report Approved.
Status: logic VERIFIED by unit test. MUTATES.

  Run 00 Base Setup first (same session) so the shared vars are available.
}
