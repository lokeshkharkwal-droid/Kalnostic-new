meta {
  name: EC16 NABL mode enabled
  seq: 17
}

script:pre-request {
  if (!bru.getVar("baseUrl")) bru.setVar("baseUrl", "http://localhost:3000/api/v1");
}

docs {
  Scenario: turn NABL ON.
Expected: branch_settings.is_nabl_tat_enabled=true; cron now manages TAT.
Status: VERIFIED live.

  Run 00 Base Setup first (same session) so the shared vars are available.
}
