meta {
  name: EC08 Approval after working hours
  seq: 9
}

script:pre-request {
  if (!bru.getVar("baseUrl")) bru.setVar("baseUrl", "http://localhost:3000/api/v1");
}

docs {
  Scenario: approve after the window has closed (clock paused).
Expected: final net = accrued total; no minutes added after Processing Time To.
Status: logic VERIFIED by unit test. MUTATES.

  Run 00 Base Setup first (same session) so the shared vars are available.
}
