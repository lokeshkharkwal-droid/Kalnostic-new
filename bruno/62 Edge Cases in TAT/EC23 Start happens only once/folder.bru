meta {
  name: EC23 Start happens only once
  seq: 24
}

script:pre-request {
  if (!bru.getVar("baseUrl")) bru.setVar("baseUrl", "http://localhost:3000/api/v1");
}

docs {
  Scenario: duplicate-start guard.
Expected: tat_start_at never changes once set; the clock is not restarted.
Status: VERIFIED live + unit test.

  Run 00 Base Setup first (same session) so the shared vars are available.
}
