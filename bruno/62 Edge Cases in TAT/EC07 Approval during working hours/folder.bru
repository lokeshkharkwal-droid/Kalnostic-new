meta {
  name: EC07 Approval during working hours
  seq: 8
}

script:pre-request {
  if (!bru.getVar("baseUrl")) bru.setVar("baseUrl", "http://localhost:3000/api/v1");
}

docs {
  Scenario: approve while the clock is running (final stop).
Expected: tat_is_running=false, tat_end_at set, net = accrued total, band recomputed.
Status: logic VERIFIED by unit test. MUTATES.

  Run 00 Base Setup first (same session) so the shared vars are available.
}
