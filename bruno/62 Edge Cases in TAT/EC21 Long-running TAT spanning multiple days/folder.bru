meta {
  name: EC21 Long-running TAT spanning multiple days
  seq: 22
}

script:pre-request {
  if (!bru.getVar("baseUrl")) bru.setVar("baseUrl", "http://localhost:3000/api/v1");
}

docs {
  Scenario: do not approve for several working days.
Expected: net = SUM of each working day's minutes; non-working time never counted.
Status: VERIFIED by unit test (accumulation).

  Run 00 Base Setup first (same session) so the shared vars are available.
}
