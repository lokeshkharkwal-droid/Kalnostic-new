meta {
  name: Delete Payment
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/payments/{{paymentId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{businessAccessToken}}
}

docs {
  Soft-delete a payment record (sets `deletedAt`). Scoped to the caller's tenant.
}
