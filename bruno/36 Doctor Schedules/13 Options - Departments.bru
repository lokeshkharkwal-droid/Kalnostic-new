meta {
  name: Options - Departments
  type: http
  seq: 13
}

get {
  url: {{baseUrl}}/doctor-schedules/options/departments
  body: none
  auth: bearer
}

auth:bearer {
  token: {{businessAccessToken}}
}

docs {
  Active department `{ id, name }` options for the tenant.
}
