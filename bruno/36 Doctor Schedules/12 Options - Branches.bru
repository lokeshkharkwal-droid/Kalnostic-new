meta {
  name: Options - Branches
  type: http
  seq: 12
}

get {
  url: {{baseUrl}}/doctor-schedules/options/branches
  body: none
  auth: bearer
}

auth:bearer {
  token: {{businessAccessToken}}
}

docs {
  Branch `{ id, name }` options for the tenant.
}
