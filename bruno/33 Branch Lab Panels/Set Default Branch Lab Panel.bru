meta {
  name: Set Default Branch Lab Panel
  type: http
  seq: 9
}

patch {
  url: {{baseUrl}}/branch-lab-panels/{{branchLabPanelId}}/default
  body: none
  auth: bearer
}

auth:bearer {
  token: {{businessAccessToken}}
}

vars:pre-request {
  branchLabPanelId: 00000000-0000-0000-0000-000000000000
}

docs {
  Mark this row the default of its variant group (used when creating a lab order).
  Clears `isDefault` on the group's other active rows in one transaction. No body.
}
