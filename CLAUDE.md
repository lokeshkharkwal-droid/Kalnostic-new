# CLAUDE.md — Kalnostics New (re-export)

> **This is not the source of truth.** The canonical rules live one level up, in
> `Master/CLAUDE.md`. This file only re-exports them so the same rules apply
> whether Claude is launched in `Master/` or works directly inside this repo.
> **Do not edit this file** — make every change in `../CLAUDE.md` so the two
> never drift.

@../CLAUDE.md
